# Changelog

## 0.3.0

- Move package ownership to Constructive Blocks.
- Keep the command engine headless and distribute its visual composition through `@constructive/command-palette`.
- Remove the runtime dependency on `@constructive-io/ui`, `cmdk`, and `motion` from the package.
